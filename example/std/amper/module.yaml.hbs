product: {{ _module.type }}

dependencies:
  {{#each _module.dependencies}}
  - {{ group }}:{{ artifact }}:{{ version }}
  {{/each}}

{{#if _module.amper}}
settings:
  {{#each _module.amper.entries}}
    {{ key }}: {{ value }}
  {{/each}}
{{/if}}