name: Kotlinx JSON
description: Handles JSON serialization
properties:
  - key: serverModules
    type: list<string>?
    hidden: true
  - key: configFormat
    type: enum { HOCON, YAML, none }
    default: HOCON