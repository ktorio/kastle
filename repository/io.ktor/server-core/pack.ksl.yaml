name: Ktor Server
description: Adds a Ktor HTTP server
properties:
  - key: serverModules
    type: list<string>?
    hidden: true
  - key: configFormat
    type: enum { HOCON, YAML, none }
    default: HOCON