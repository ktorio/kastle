platforms: [jvm, android]

dependencies:
  - $compose.foundation!
  - $compose.material3!
  - $compose.preview!
  - $compose.components.resources!

gradle:
  plugins:
    - kotlin.multiplatform
    - compose.multiplatform
    - compose.compiler
    - android.library

amper:
  compose: enabled
