platform: android

dependencies:
  - ../sharedUI
  - $libs.androidx.activity.compose

amper:
  compose: enabled

gradle:
  plugins:
    - $libs.androidApplication
    - $libs.composeMultiplatform
    - $libs.composeCompiler

sources:
  - path: android.gradle.kts
    target: slot://org.gradle/gradle/buildRoot
