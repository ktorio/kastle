platform: android

dependencies:
  - ../shared
  - $libs.android.activity.compose

amper:
  compose: enabled

gradle:
  plugins:
    - compose.multiplatform
    - compose.compiler
    - compose.hotReload
    - android.application
