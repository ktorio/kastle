platform: jvm

dependencies:
  - ../shared
  - $compose.desktop.currentOs

gradle:
  plugins:
    - compose.multiplatform
    - compose.compiler
    - compose.hotReload

amper:
  compose: enabled

sources:
  - path: desktop.gradle.kts
    target: slot://org.gradle/gradle/buildRoot