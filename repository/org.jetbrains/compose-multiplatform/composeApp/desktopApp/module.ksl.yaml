platform: jvm

dependencies:
  - ../sharedUI
  - $composeLibs.desktop.currentOs
  - $libs.kotlinx.coroutinesSwing

gradle:
  plugins:
    - $libs.composeMultiplatform
    - $libs.composeCompiler

amper:
  compose: enabled

sources:
  - path: desktop.gradle.kts
    target: slot://org.gradle/gradle/buildRoot
