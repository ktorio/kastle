platform: jvm

dependencies:
  - ../shared
  - $compose.desktop.currentOs

gradle:
  plugins:
    - compose.multiplatform
    - compose.compiler

amper:
  compose: enabled

sources:
  - target: slot://org.gradle/gradle/gradleConfigurations
    path: config.gradle.kts