platforms: [jvm, android, ios, js, wasmJs]

dependencies:
  - $compose.foundation!
  - $compose.material3!
  - $compose.preview!
  - $compose.components.resources!

gradle:
  plugins:
    - compose.multiplatform
    - compose.compiler
    - android.library

amper:
  compose: enabled
