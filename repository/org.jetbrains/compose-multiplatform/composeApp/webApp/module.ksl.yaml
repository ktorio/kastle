platform: web

dependencies:
  - ../sharedUI

gradle:
  plugins:
    - compose.multiplatform
    - compose.compiler

amper:
  compose: enabled

propertyValues:
  - key: org.gradle/gradle/isExecutable
    value: true