platform: web

dependencies:
  - ../sharedUI

gradle:
  plugins:
    - $libs.composeMultiplatform
    - $libs.composeCompiler

amper:
  compose: enabled

propertyValues:
  - key: org.gradle/gradle/isExecutable
    value: true
