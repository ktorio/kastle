platform: web

dependencies:
  - ../sharedUI

gradle:
  plugins:
    - composeMultiplatform
    - composeCompiler

amper:
  compose: enabled

propertyValues:
  - key: org.gradle/gradle/isExecutable
    value: true
