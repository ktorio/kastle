platform: web

dependencies:
  - ../sharedUI

gradle:
  plugins:
    - $libs.plugins.composeMultiplatform
    - $libs.plugins.composeCompiler

amper:
  compose: enabled

propertyValues:
  - key: org.gradle/gradle/isExecutable
    value: true
